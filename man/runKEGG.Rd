% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FuncEnrichment.R
\name{runKEGG}
\alias{runKEGG}
\title{Perform KEGG pathway enrichment analysis}
\usage{
runKEGG(
  gene,
  use_padj = TRUE,
  organism = "hsa",
  keyType = "SYMBOL",
  OrgDb = "org.Hs.eg.db",
  pvalueCutoff = 0.05,
  pAdjustMethod = "BH",
  qvalueCutoff = 1,
  minGSSize = 10,
  maxGSSize = 500,
  use_internal_data = FALSE,
  ...
)
}
\arguments{
\item{gene}{A character vector of gene symbols (e.g., c("TP53", "BRCA1", "BRCA2"))}

\item{use_padj}{Logical, whether to use adjusted p-value (p.adjust) for filtering.
If TRUE, filters by p.adjust; if FALSE, filters by pvalue. Default: TRUE}

\item{organism}{Character string specifying KEGG organism code (e.g., "hsa" for human,
"mmu" for mouse, "rno" for rat). Default: "hsa"}

\item{keyType}{Character string, input gene ID type. Default: "SYMBOL"}

\item{OrgDb}{Character string specifying the organism database for ID conversion
(e.g., "org.Hs.eg.db"). Only used when keyType is "SYMBOL".
Default: "org.Hs.eg.db"}

\item{pvalueCutoff}{Numeric, p-value cutoff for enrichment analysis. Default: 0.05}

\item{pAdjustMethod}{Character string, method for p-value adjustment. Options include
"BH" (Benjamini-Hochberg), "bonferroni", "fdr", etc. Default: "BH"}

\item{qvalueCutoff}{Numeric, q-value cutoff for enrichment analysis. Default: 1}

\item{minGSSize}{Minimum gene set size. Default: 10}

\item{maxGSSize}{Maximum gene set size. Default: 500}

\item{use_internal_data}{Logical, whether to use KEGG.db (deprecated). Default: FALSE}

\item{...}{Additional parameters passed to enrichKEGG()}
}
\value{
An enrichResult object from clusterProfiler containing KEGG enrichment results.
        Use as.data.frame() to convert to a data frame.
}
\description{
Performs KEGG pathway enrichment analysis using clusterProfiler.
             Automatically converts gene symbols to Entrez IDs and performs enrichment
             for KEGG pathways.
}
\examples{
\dontrun{
# Basic usage with default parameters
genes <- c("TP53", "BRCA1", "BRCA2", "ATM", "CHEK2")
kegg_result <- runKEGG(genes)

# Use p-value instead of adjusted p-value
kegg_result <- runKEGG(genes, use_padj = FALSE)

# Analyze mouse genes
kegg_result <- runKEGG(genes, organism = "mmu", OrgDb = "org.Mm.eg.db")

# Custom parameters
kegg_result <- runKEGG(genes, pvalueCutoff = 0.01, qvalueCutoff = 0.1)

# Convert to data frame
kegg_df <- as.data.frame(kegg_result)
}
}
