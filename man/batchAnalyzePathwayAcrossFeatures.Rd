% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FuncGSVA.R
\name{batchAnalyzePathwayAcrossFeatures}
\alias{batchAnalyzePathwayAcrossFeatures}
\title{Batch analyze a single pathway (gene list) across multiple features}
\usage{
batchAnalyzePathwayAcrossFeatures(
  dromaset_object,
  gene_list,
  pathway_name,
  feature_type = "drug",
  feature_names = NULL,
  gsva_method = c("gsva", "ssgsea", "zscore", "plage"),
  data_type = "all",
  tumor_type = "all",
  overlap_only = FALSE,
  zscore = TRUE,
  min_size = 5,
  show_progress = TRUE,
  test_top_n = NULL,
  cores = 1
)
}
\arguments{
\item{dromaset_object}{Either a DromaSet or MultiDromaSet object}

\item{gene_list}{Character vector of gene symbols defining the pathway}

\item{pathway_name}{Name for this pathway (used in output)}

\item{feature_type}{Type of features to test against: "drug" (default), "mRNA", "cnv", 
"mutation_gene", "mutation_site", "fusion", "meth", "proteinrppa", "proteinms"}

\item{feature_names}{Optional: character vector of specific feature names to test.
If NULL, tests all available features of the specified type}

\item{gsva_method}{GSVA method to use: "gsva" (default), "ssgsea", "zscore", or "plage"}

\item{data_type}{Filter by data type ("all", "CellLine", "PDO", "PDC", "PDX")}

\item{tumor_type}{Filter by tumor type ("all" or specific tumor types)}

\item{overlap_only}{For MultiDromaSet, whether to use only overlapping samples (default: FALSE)}

\item{zscore}{Logical, whether to apply z-score normalization to feature data (default: TRUE)}

\item{min_size}{Minimum number of genes in gene_list that must be present in expression data (default: 5)}

\item{show_progress}{Logical, whether to show progress messages (default: TRUE)}

\item{test_top_n}{Integer, number of top features to test (default: NULL for all features)}

\item{cores}{Number of CPU cores to use for parallel processing (default: 1 for sequential).
Uses future + furrr with task chunking for efficiency when cores > 1}
}
\value{
A data frame with columns: name (feature name), p_value, effect_size, n_datasets, q_value
}
\description{
Given a single gene list (pathway), calculates GSVA score and tests associations
             with multiple features (drugs or omics). Similar to batchFindSignificantFeatures
             but uses pathway-level GSVA scores instead of single gene expression.
}
\examples{
\dontrun{
# Define a custom gene list
my_genes <- c("TP53", "BRCA1", "BRCA2", "ATM", "CHEK2", "RAD51", "PALB2")

# Test pathway association with all drugs
gCSI <- createDromaSetFromDatabase("gCSI", "path/to/droma.sqlite")
results <- batchAnalyzePathwayAcrossFeatures(gCSI, my_genes, "DNA_Repair", 
                                             feature_type = "drug")

# Test pathway association with specific drugs
results <- batchAnalyzePathwayAcrossFeatures(gCSI, my_genes, "DNA_Repair",
                                             feature_type = "drug",
                                             feature_names = c("Olaparib", "Cisplatin"))

# Test pathway association with all genes (mRNA)
results <- batchAnalyzePathwayAcrossFeatures(gCSI, my_genes, "DNA_Repair",
                                             feature_type = "mRNA", test_top_n = 1000)

# Test pathway association with mutation status
results <- batchAnalyzePathwayAcrossFeatures(gCSI, my_genes, "DNA_Repair",
                                             feature_type = "mutation_gene")

# With parallel processing
results <- batchAnalyzePathwayAcrossFeatures(gCSI, my_genes, "DNA_Repair",
                                             feature_type = "drug", cores = 8)
}
}
