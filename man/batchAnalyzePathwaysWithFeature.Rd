% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FuncGSVA.R
\name{batchAnalyzePathwaysWithFeature}
\alias{batchAnalyzePathwaysWithFeature}
\title{Batch analyze GSVA scores for multiple pathways against a single feature}
\usage{
batchAnalyzePathwaysWithFeature(
  dromaset_object,
  gsva_scores,
  feature_name,
  feature_type = "drug",
  gene_set_names = NULL,
  data_type = "all",
  tumor_type = "all",
  overlap_only = FALSE,
  cores = 1,
  zscore = TRUE,
  show_progress = TRUE
)
}
\arguments{
\item{dromaset_object}{Either a DromaSet or MultiDromaSet object}

\item{gsva_scores}{GSVA scores from calculateGSVAScores(). Can be a matrix (single dataset) 
or a list of matrices (multiple datasets)}

\item{feature_name}{Name of the feature to analyze (drug name or omics feature name)}

\item{feature_type}{Type of feature: "drug" (default), or omics types like "mRNA", "mutation_gene", etc.}

\item{gene_set_names}{Optional: character vector of gene set names to analyze. 
If NULL, analyzes all gene sets in gsva_scores}

\item{data_type}{Filter by data type ("all", "CellLine", "PDO", "PDC", "PDX")}

\item{tumor_type}{Filter by tumor type ("all" or specific tumor types)}

\item{overlap_only}{For MultiDromaSet, whether to use only overlapping samples (default: FALSE)}

\item{cores}{Number of CPU cores to use for parallel processing (default: 1).
Uses future + furrr with task chunking for efficiency when cores > 1}

\item{zscore}{Logical, whether to apply z-score normalization (default: TRUE)}

\item{show_progress}{Logical, whether to show progress messages (default: TRUE)}
}
\value{
A data frame with columns: name (gene set name), p_value, effect_size, n_datasets, q_value
}
\description{
Performs batch analysis of associations between GSVA scores for multiple gene sets
             and a single feature (drug or omics), returning a data frame with statistics for each gene set.
             Use this when you have pre-computed GSVA scores for multiple pathways and want to test
             each pathway against the same feature.
}
\examples{
\dontrun{
# Calculate GSVA scores for all Hallmark gene sets
gCSI <- createDromaSetFromDatabase("gCSI", "path/to/droma.sqlite")
all_gene_sets <- getAvailableGeneSets()
gsva_scores <- calculateGSVAScores(gCSI, all_gene_sets, method = "gsva")

# Batch analyze associations with a drug
results <- batchAnalyzePathwaysWithFeature(gCSI, gsva_scores, "Bortezomib",
                                          feature_type = "drug", cores = 4)

# Batch analyze associations with a gene expression
results <- batchAnalyzePathwaysWithFeature(gCSI, gsva_scores, "TP53", feature_type = "mRNA")

# Batch analyze associations with mutation status
results <- batchAnalyzePathwaysWithFeature(gCSI, gsva_scores, "KRAS", feature_type = "mutation_gene")
}
}
