% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FuncGSVA.R
\name{calculateGSVAScores}
\alias{calculateGSVAScores}
\title{Calculate GSVA scores for gene sets}
\usage{
calculateGSVAScores(
  dromaset_object,
  gene_sets,
  method = c("gsva", "ssgsea", "zscore", "plage"),
  data_type = "all",
  tumor_type = "all",
  overlap_only = FALSE,
  min_size = 5,
  max_size = Inf,
  gmt_file = NULL,
  ...
)
}
\arguments{
\item{dromaset_object}{Either a DromaSet or MultiDromaSet object}

\item{gene_sets}{Either a named list of gene sets (each element is a character vector of gene symbols), 
or a character vector of gene symbols for a single gene set, 
or a character string naming a predefined gene set}

\item{method}{GSVA method to use: "gsva" (default), "ssgsea", "zscore", or "plage"}

\item{data_type}{Filter by data type ("all", "CellLine", "PDO", "PDC", "PDX")}

\item{tumor_type}{Filter by tumor type ("all" or specific tumor types)}

\item{overlap_only}{For MultiDromaSet, whether to use only overlapping samples (default: FALSE)}

\item{min_size}{Minimum number of genes in a gene set that must be present in the expression data (default: 5)}

\item{max_size}{Maximum number of genes in a gene set (default: Inf)}

\item{gmt_file}{Optional path to GMT file if using predefined gene set names}

\item{...}{Additional parameters passed to GSVA::gsva()}
}
\value{
A list containing GSVA scores (gene sets x samples matrix) for each dataset
}
\description{
Main function for calculating gene set enrichment scores using GSVA methods
}
\examples{
\dontrun{
# Using DromaSet with custom gene list
gCSI <- createDromaSetFromDatabase("gCSI", "path/to/droma.sqlite")
custom_genes <- c("TP53", "BRCA1", "BRCA2", "ATM", "CHEK2")
scores <- calculateGSVAScores(gCSI, custom_genes, method = "ssgsea")

# Using predefined gene set name
scores <- calculateGSVAScores(gCSI, "HALLMARK_APOPTOSIS", method = "gsva")

# Using multiple gene sets as a list
gene_sets_list <- list(
  apoptosis = c("TP53", "BAX", "BCL2"),
  cell_cycle = c("CDK1", "CDK2", "CCNB1")
)
scores <- calculateGSVAScores(gCSI, gene_sets_list, method = "plage")

# Using MultiDromaSet
multi_set <- createMultiDromaSetFromDatabase(c("gCSI", "CCLE"), "path/to/droma.sqlite")
scores <- calculateGSVAScores(multi_set, "HALLMARK_APOPTOSIS")
}
}
