% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FuncCTRDB.R
\name{batchFindClinicalSigResponse}
\alias{batchFindClinicalSigResponse}
\title{Batch analysis of clinical drug response for multiple omics features}
\usage{
batchFindClinicalSigResponse(
  select_omics,
  select_drugs,
  data_type = "all",
  tumor_type = "all",
  cores = 1
)
}
\arguments{
\item{select_omics}{Character vector of omics feature names (e.g., gene symbols)}

\item{select_drugs}{Character string specifying the drug name}

\item{data_type}{Filter by data type ("all", "CellLine", "PDO", "PDC", "PDX")}

\item{tumor_type}{Filter by tumor type ("all" or specific tumor types)}

\item{cores}{Number of CPU cores (default: 1). Parallelization not implemented; kept for API consistency.}
}
\value{
Data frame with p_value, effect_size, n_datasets, name, q_value. For multi-dataset cases, meta-analysis is used; for single-dataset cases, Wilcoxon test and Cliff's Delta are used (consistent with metaCalcConDis).
}
\description{
For each omics feature, runs analyzeClinicalDrugResponse against a drug,
  extracts meta-analysis results, and returns a data frame in the same format as batchFindSignificantFeatures.
}
\examples{
\dontrun{
connectCTRDatabase("path/to/ctrdb.sqlite")
results <- batchFindClinicalSigResponse(
  select_omics = c("EPB41L4B", "FAM83H", "PLEKHA1"),
  select_drugs = "Bortezomib"
)
}
}
